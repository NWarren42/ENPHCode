#include "password2.h" //  Do not change this
#include <stdio.h>

int main() {
    int x, y;
    x = 15;
    y = 7;
    float z = (x + 4) / y;
    printf("Value of z is %.2f\n", z);
    completion(z); // Do not change this
    return 0;
}