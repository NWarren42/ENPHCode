#include <stdio.h>

int main() {
    float x, y, z;
    x = 15;
    printf("Enter two numbers:\n");
    scanf("%f %f", &y, &z);

    /* expression(s) using assignment shorthands */
    printf("New x value = %.3f\n", x);
}