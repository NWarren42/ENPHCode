#include <stdio.h>

int main() {
    int x, y;
    x = 7;
    y = 30;

    if(x >= y)
        printf("y is not larger than x.\n");
    /* else if block #1 */
    /* else if block #2 */
    else
        printf("y is a lot larger than x!\n");
}