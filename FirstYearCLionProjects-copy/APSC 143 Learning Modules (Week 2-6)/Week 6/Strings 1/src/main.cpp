#include <stdio.h>

int main() {
    char word[5] = /* Initialize word */;
    int average = 0;
    int wordSize = sizeof(word) / sizeof(word[0]) - 1;
    /* Find average letter */
    printf(/* Output word */);
    printf("Average letter = '%c'\n", average);
}