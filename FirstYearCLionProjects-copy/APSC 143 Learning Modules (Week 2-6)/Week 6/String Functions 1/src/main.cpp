#include <stdio.h>
/* Include the string library */

int main() {
    char word[] = "irrevocably";
    int average = 0;
    /* Determine average of word */
    printf("Word = \"%s\"\n", word);
    printf("Average letter = '%c'\n", average);
}