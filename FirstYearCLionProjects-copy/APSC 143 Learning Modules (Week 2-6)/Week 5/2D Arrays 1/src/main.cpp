#include <stdio.h>

int main() {
    int nums[3][4] = /* initialize whole array */;
    for(int row = 0; row < 3; row++) {
        for(int col = 0; col < 4; col++) {
            /* print each value */
        }
        printf("\n");
    }
}