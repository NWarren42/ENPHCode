#include <stdio.h>

#define LENGTH 5

int main() {
    printf("Enter float values:\n");
    /* Declare the nums array */
    /* element storing loop */
    for(int i = 0; i < LENGTH; i++)
        printf("%.2f\t", nums[i]);
    printf("\n");
}