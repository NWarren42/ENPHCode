#include "password4.h" // Do not change this
#include <stdio.h>

int main() {
    int upTo, i;
    printf("Enter a positive integer:\n");
    scanf("%d", &upTo);
    /* nested loop here */
    completion(i, upTo);
}